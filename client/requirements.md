## Packages
(none needed)

## Notes
- Floating WhatsApp button relies on the provided phone number (+8801533262758).
- The frontend assumes the presence of Shadcn UI components in `client/src/components/ui/` based on the package list, but also uses standard Tailwind classes for highly customized, premium layouts.
- Assets from the design system are assumed to be available at `/images/`. Fallbacks to Lucide icons are implemented.
